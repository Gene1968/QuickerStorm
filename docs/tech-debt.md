# Tech Debt Log — quickerSTORM

> Shortcuts and known issues to revisit. Add rows as they appear; review during planning.

| ID | Description | Introduced | Why Accepted | Priority | Effort |
| --- | --- | --- | --- | --- | --- |
| ~~var-region-terrain~~ | FIXED 2026-05-30: 32×32 patch IDCT + 32-bit patch IDs implemented for LandExtended (typeB=0x4D). NeverWorld 512m terrain now decodes. NOT live-verified against a running grid — confirm on next NeverWorld session. (typeB=0x37 = OSGrid sea-floor, correctly skipped, unrelated.) | 2026-05-25 | — | low | — |
| ~~terrain-missing-patches~~ | RESOLVED 2026-05-27: OSGrid sea-floor layer (typeB=0x37) was being classified as LAND and overwriting real ground at patch (0,0). Fix in terrain-codec.ts removes 0x37 from `isLand`. All four bitstream-desync hypotheses were wrong. Instrumentation (worldStore.patchReceived Set, server keys+cache-size logging, codec OOB warnings) retained for regressions. See `memory/terrain-decoder-missing-patches.md`. | 2026-05-27 | — | — | — |
| terrain-mesh-mirror | Terrain mesh write path uses `iy = ry - slY` to match PlaneGeometry orientation. Pre-fix the mesh was N-S mirrored (invisible in flat regions; surfaced as "walking on water" / "sinking into hill"). Any new code touching `terrainMesh.geometry.attributes` must keep this invariant. Documented in `memory/threejs-rendering-decisions.md`. | 2026-05-27 | Constraint, not debt | low | — |
| map-stale-region-on-walk | Map purple dot uses `session.regionX/Y` which only updates on TeleportFinish. Walking across a region boundary triggers cross-sim handshake (DisableSimulator + new UseCircuitCode) that we don't yet handle — dot lags behind real region. | 2026-05-29 | Cross-sim walk is a separate Phase 2/3 feature, map TP path works correctly | medium | 6h |
| map-prim-diagnostics | `server/handlers/lludp.ts` still ships `prim-ids-snapshot.txt` writer, per-packet hex dumps in `→ MapBlockRequest`, and verbose `[PrimDiag]` logging. Useful while prim/map paths are flaky; remove or gate behind env var once stable. | 2026-05-29 | Active diagnostic — see [[phase2-prim-rendering-resolved]] | low | 1h |
| map-no-localstorage-cache | `mapStore.regions` populated each session — reopening Map after login re-queries every visible chunk. 60s in-session TTL helps but doesn't survive reload. Persist to localStorage with TTL for faster reopen. | 2026-05-29 | Phase 2 polish deferred — see [[map-phase2-shipped]] | low | 2h |
| friends-live-verify | Friends/Contacts tab in ConversationsFloater is fully built (search, online list, rights toggles, add-by-name, remove). NOT live-tested. Remove this row after confirming on a live grid. | 2026-05-30 | Implemented but unverified | medium | 0.5h |
| appearance-no-bake | AppearanceFloater edits local color/skin/hair in avatarStore; no AgentSetAppearance or baked texture pipeline. Outfits tab is a shell. Wiring bake risks global appearance corruption — needs test-grid validation before enabling. | 2026-05-31 | Risk management; Phase 3 late item | medium | 8h+ |
| coarse-location-minimap | CoarseLocationUpdate forwarding dropped at lludp.ts; minimap uses full ObjectUpdate dots only. Region-wide avatar dots unreliable for non-nearby avies. | 2026-05-31 | Separate server work needed; minimap otherwise functional | medium | 3h |
| var-region-live-verify | var-region-terrain fix (2026-05-30) not confirmed on a running NeverWorld session. Remove this row after first successful live test. | 2026-05-30 | Fix committed but unverified | low | 0.5h |
| deferred-wind-layer | LayerData type 0x57 ('W') = wind field. Same bit-packed codec as terrain (BitReader + IDCT), but payload encodes 2D wind velocity vectors across the region. Skipped in terrain-codec.ts. Needed for: particle system wind influence, swimming/flying animation drift. Ref: FS indra/llmessage/patch_code.cpp `WIND_LAYER_CODE`; libopenmetaverse TerrainCompressor.cs. | 2026-06-01 | No visual consumer yet | medium | 4h |
| deferred-cloud-layer | LayerData type 0x43 ('C') = cloud field. Same codec. Payload = cloud density + wind scalars per patch. Skipped. Needed for: sky cloud animation overlay. Low priority until sky rendering exists. Ref: FS indra/llmessage/patch_code.cpp `CLOUD_LAYER_CODE`. | 2026-06-01 | No visual consumer | low | 3h |
| deferred-water-layer | LayerData type 0x38 ('8') = water layer. Same codec. Payload = wave parameters (height, normal, scale per patch). Skipped. Our custom water ShaderMaterial already handles ripple. When upgrading to sim-driven waves: decode this. Ref: FS indra/llvfs/lldir.cpp water layer constants. | 2026-06-01 | Custom shader suffices for now | low | 3h |
| perface-pbr-skip | Mesh per-face multi-material (useWorldEngine `buildFaceMaterials`) and PBR (`defaultPbrMaterial`, GLTF) are mutually exclusive per mesh: a multi-textured mesh skips the PBR block (per-face diffuse textures win). A mesh that is BOTH multi-face AND PBR-material loses its PBR (metalness/roughness/normal). Rare combo (legacy multi-face mesh + new PBR ExtraParam). To unify: build the per-face array from PBR descriptors too. Ref: useWorldEngine PBR block gated `!meshMulti`. | 2026-06-07 | Rare; per-face diffuse is the more common need | low | 4h |
| worker-bake-placeholder-forever | Prim geometry now bakes async in a Web Worker (useMeshBaker); a unit-cube placeholder shows until the swap. If a bake promise never resolves (worker drops a job, or a reply's `id` isn't in `pending`), the prim stays a unit cube forever — a new failure mode the old synchronous build didn't have (it either built or threw+counted). `useMeshBaker.dispose()`/`killWorker()` settle all pending via sync fallback, so worker DEATH is covered; this is only the silent-lost-reply case. Acceptable degradation; add an idle re-bake sweep if observed live. Ref: `useWorldEngine.js` applySwap; `useMeshBaker.js`. | 2026-06-07 | Strictly better than a crash; not observed | low | 2h |
| deferred-particles | ObjectUpdate tail field `PSBlock` (Variable1, 1-byte length prefix): standard SL = 86 bytes (libopenmetaverse/FS LLPartData), OpenSim extended = up to ~192 bytes (ESP/AdvancedParticle system). Currently skipped via skipVar1 — prim renders without particles. Extended 192-byte format causes consistent tail-OOB on localId=675308947 (pcode=9). To implement: decode PSBlock → THREE.Points emitter per object. Ref: FS indra/llcharacter/llpartdata.cpp; libomv Particle.cs. | 2026-06-01 | Phase 3+ visual feature | medium | 16h+ |
| render-draw-calls | ~9k individual meshes = ~9k draw calls → 30-43ms frames + steady rAF-violation noise on dense regions (the C2/C3 residual). Fix = instanced/merged geometry for static prims. Nominated as the next render project — the new memory culler keeps 3× more objects resident, so this now dominates frame time. | 2026-06-11 | Big project; scene correctness came first | high | 24h+ |
| tex-netqueue-closures | useTextureFetch `netQueue` holds a closure per queued fetch (~8-10k on a dense cold load). Each is small but the pile is unbounded; consider storing bare uuids + one runner. | 2026-06-11 | Minor heap cost; intake budget bounds the practical impact | low | 2h |
| gpu-texture-dispose | `pruneTexturesLRU` releases Map refs only (never `dispose()`) — GPU-side memory of textures still bound to evicted meshes' materials needs ref-counting to free. JS-heap side is handled. Ref: comment above pruneTexturesLRU. | 2026-06-11 | JS-heap was the crash vector; GPU pressure not yet observed | medium | 6h |
| shader-precompile-frame | One-time ~6.8s rAF frame after dense-scene load = synchronous shader precompile of the whole scene. Move to `renderer.compileAsync` or idle-chunked compile. | 2026-06-11 | Once per region load; trade-off was render-flicker fix | low | 3h |
| server-asset-fifo-no-cancel | Server asset pipeline has no cancel for requests the client has timed out on. Largely mitigated by assetMemo coalescing + client cache-recheck (re-asks are now rare); revisit only if [AssetMemo] inflight stays high. | 2026-06-11 | Memo made it near-moot | low | 3h |
| instant-load-geometry-cache | Every reload re-bakes ~10k prim geometries through the single worker at ~30-40/s ≈ 4-5 min CPU-bound — THE reason "cached" ≠ "instant". IDB caches assets (ingredients), not baked output. Fix: persist worker bake results (BufferGeometry-ready arrays, keyed by shape+scale hash) in IDB; reload = deserialize + GPU upload. Companion to the webp-texture-cache plan (see its 2026-06-11 addendum). | 2026-06-11 | Needs own spec; webp plan shipped first | high | 16h+ |
| crc-probe-audit | On reload the client re-requested asked≈19.9k of ~24k objects from the sim — the CRC/CacheID object-cache probe path isn't suppressing the sim re-feed it was built for (suspects: region-restart stale localIds, cache-clear interplay, probe/backlog ordering). Audit with a warm IDB cache and count suppressed vs asked. | 2026-06-11 | Observed during cull-spiral session; not the dominant reload cost | medium | 6h |
| geom-cache-idb-version-handlers | `geomCache.js` `openDb` has no `onblocked`/`onversionchange` handlers. The first `DB_VERSION` bump will hang the upgrade in multi-tab sessions. Add both handlers before bumping (see `objectCache.js` for the pattern). | 2026-06-12 | No version bump yet; DB_VERSION is still 1 | low | 1h |
| tex-refresh-stale-queue | `refreshTextures(uuids)` clears in-memory caches but a uuid already sitting in `buildQueue`/`uploadQueue` (useTextureFetch.js two-phase pump) can still resolve its stale blob into `cache.set()` after the refresh cleared it, clobbering the freshly-fetched texture (nondeterministic order). Pre-existing (the old single-phase async `_processBuild` had the same hazard); slightly widened by the second queue. Fix: tag queued jobs with a refresh epoch and drop stale ones on upload. | 2026-06-16 | Pre-existing race; slightly widened by #11 Pass 2 two-phase pump. Flagged by code review (#11 Pass 2). | low | 2h |
| stopWarpWatch-leak | `stopWarpWatch` (useWorldEngine.js ~line 237) is created but never called in `onUnmounted` — the watcher outlives the engine instance on every unmount. Teardown list at ~line 4748 covers four other stops but omits this one. Fix: add `stopWarpWatch()` to the unmount stop-watch cluster. | 2026-06-17 | Flagged by code-quality review of highlight-wiring task (commit 4dfdd33); pre-existing. | low | 0.5h |

