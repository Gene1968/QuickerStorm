NODE_ENV=development
VITE_APP_BASE=/
VITE_APP_ENV=development
VITE_DEBUG=true

ALLOWED_ORIGIN=https://genebiondo.com,https://quickerstorm.com,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177,http://localhost:5178

# ── WebSocket Server ─────────────────────────────────────────────────────
# Pick ONE:
#   Local:   ws://localhost:8787       (run `npm run dev:server` in a second terminal)
#   Staging: wss://signal-proxy-staging.up.railway.app  (no local server needed)
VITE_SIGNAL_URL=ws://localhost:8787
